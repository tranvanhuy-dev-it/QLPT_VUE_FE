<template>
  <button
    :type="type"
    :disabled="disabled || loading"
    v-bind="$attrs"
    :class="[
      'inline-flex items-center justify-center gap-1.5 rounded-lg transition-all duration-150 cursor-pointer font-semibold select-none',
      variant !== 'custom' ? (size === 'sm' ? 'px-2.5 py-1.5 text-xs' : 'px-4 py-2 text-sm') : '',
      variantStyles[variant] || variantStyles.primary,
      (disabled || loading) ? 'opacity-60 cursor-not-allowed' : ''
    ]"
  >
    <svg
      v-if="loading"
      class="animate-spin h-4 w-4 text-current shrink-0"
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
    >
      <circle
        class="opacity-25"
        cx="12"
        cy="12"
        r="10"
        stroke="currentColor"
        stroke-width="4"
      ></circle>
      <path
        class="opacity-75"
        fill="currentColor"
        d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
      ></path>
    </svg>
    <slot></slot>
  </button>
</template>

<script src="./FormButton.js"></script>
